

Initialize the local directory as a Git repository.
$ git init -b Develop
Add the files in your new local repository. This stages them for the first commit.
$ git add .
# Adds the files in the local repository and stages them for commit. To unstage a file, use 'git reset HEAD YOUR-FILE'.
Commit the files that you've staged in your local repository.

$ git commit -m "First commit"      git commit -m "bai-frontend"
# Commits the tracked changes and prepares them to be pushed to a remote repository. To remove this commit and modify the file, use 'git reset --soft HEAD~1' and commit and add the file again.
At the top of your repository on GitHub.com's Quick Setup page, click  to copy the remote repository URL.
Copy remote repository URL field
In Terminal, add the URL for the remote repository where your local repository will be pushed.
$ git remote add origin  <REMOTE_URL> 
$ git remote add origin  https://github.com/toby-keyman/bai-web-admin.git

# Sets the new remote
$ git remote -v
# Verifies the new remote URL
Push the changes in your local repository to GitHub.com.
$ git push -u origin Develop
# Pushes the changes in your local repository up to the remote repository you specified as the origin


# git 에러 warning: adding embedded git repository
find . -mindepth 2 -name '.git' -prune -exec rm -rf {} +

find . -mindepth 2 -name '.git'
rm -rf ./bai-frontend/.git



git submodule deinit -f bai-frontend


디렉토리 rename -> git init  -> git add --all ->...





[Python lib]
Initialize the local directory as a Git repository.
$ git init -b main
Add the files in your new local repository. This stages them for the first commit.
$ git add .
# Adds the files in the local repository and stages them for commit. To unstage a file, use 'git reset HEAD YOUR-FILE'.
Commit the files that you've staged in your local repository.

$ git commit -m "First commit"      git commit -m "bai-frontend"
# Commits the tracked changes and prepares them to be pushed to a remote repository. To remove this commit and modify the file, use 'git reset --soft HEAD~1' and commit and add the file again.
At the top of your repository on GitHub.com's Quick Setup page, click  to copy the remote repository URL.
Copy remote repository URL field
In Terminal, add the URL for the remote repository where your local repository will be pushed.
$ git remote add origin  <REMOTE_URL> 
$ git remote add origin  https://github.com/toby-keyman/PythonDataScience.git

# Sets the new remote
$ git remote -v
# Verifies the new remote URL
Push the changes in your local repository to GitHub.com.
$ git push -u origin main
# Pushes the changes in your local repository up to the remote repository you specified as the origin


# git 에러 warning: adding embedded git repository
find . -mindepth 2 -name '.git' -prune -exec rm -rf {} +

find . -mindepth 2 -name '.git'
rm -rf ./bai-frontend/.git



git submodule deinit -f bai-frontend


디렉토리 rename -> git init  -> git add --all ->...

